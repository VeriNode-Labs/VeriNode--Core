# Formatting configuration for the VeriNode ROSCA contracts workspace.
# Values match rustfmt's stable defaults so `cargo fmt` output is explicit and
# reproducible across contributor machines and CI.
edition = "2021"
max_width = 100
tab_spaces = 4
newline_style = "Unix"
use_field_init_shorthand = true
